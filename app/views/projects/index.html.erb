<%= render partial: 'layouts/sidebar', locals: {projects_active: true} %>

<% @projects.reverse.each_with_index do |project, i| %>
  <%= render partial: "project", locals: {project: project, left: i % 2 == 0} %>
<% end %>

