<%= render partial: 'layouts/sidebar', locals: {home_active: true} %>

<% @combined.each_with_index do |item, i| %>
  <%= render partial: item.to_partial_path, locals: {photo_collection: item, photo: item, project: item, left: (i % 2) == 0} %>
<% end %>
